const authRoutes = require("./auth.routes");

/**
 * ==========================================================
 * Auth Module Index
 * ==========================================================
 * Re-exports Auth Module Express Router.
 * ==========================================================
 */

module.exports = authRoutes;
